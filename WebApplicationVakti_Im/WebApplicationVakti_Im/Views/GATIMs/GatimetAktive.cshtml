@model IEnumerable<WebApplicationVakti_Im.Models.GATIM>

@{
    ViewBag.Title = "GatimetAktive";
}

<h2>GatimetAktive</h2>

<p>
   
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.emri)
        </th>
      
        <th>
            @Html.DisplayNameFor(model => model.foto)
        </th>
        
     
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.emri)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.foto)
        </td>
      
        <td>
           
            @Html.ActionLink("Details", "Details", new { id=item.gatim_id }) 
        </td>
    </tr>
}

</table>
